// code
const record = [
    {year: "2015", result: "W"},
    {year: "2014", result: "N/A"},
    {year: "2013", result: "L"},
    //...
]
const sadly = 'undefined';


function superbowlWin(record) {
    for(const list in record) {
    if (list.result === "W");
    return list.year;
    
}
    {record.find(superbowlWin);
    }
}